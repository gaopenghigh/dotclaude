# Claude Configuration Directory

My personal Claude Code settings.

Usage:
1. Clone this repo, rename it to `~/.claude`.
2. Install Claude Code.
3. Install uv (a Python environment management tool).
4. Start a terminal and run `bash start_github_api_proxy.sh` and keep it running (put it in background or a tmux session).
